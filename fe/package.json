{
  "name": "quote.cafe",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://quote.cafe/",
  "devDependencies": {
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "aws-serverless-express": "^1.2.0",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "whatwg-fetch": "^2.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "mocks": "node ./src/mock-server.js",
    "build": "react-scripts build",
    "ci-build": "CI=true npm run build",
    "test": "react-scripts test --env=jsdom --coverage",
    "test-watch": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
